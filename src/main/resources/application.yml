spring:
  datasource:
    url: jdbc:mysql://localhost:3306/estudo_spring?useTimezone=true&serverTimezone=UTC
    username: root
    password: salvador1
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    flyway:
      baselineOnMigrate: true
      locations=classpath: db/migration